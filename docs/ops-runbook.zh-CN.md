# 运维手册

## 连接状态

- `online`：隧道已连接，网关健康检查通过。
- `degraded`：部分连接或瞬时故障。
- `offline`：连续失败次数超过阈值。

## 紧急断开

1. 打开「危险区域」。
2. 点击「紧急断开」。
3. 确认隧道状态为 `disconnected`。
4. 确认任务循环已停止。

## SSH 诊断

- 确认「连接」页面中的主机、用户名、密钥路径正确。
- 确认本地端口和远程端口与网关配置一致。
- 使用 `get_tunnel_status` 命令查看状态和最近的错误信息。
