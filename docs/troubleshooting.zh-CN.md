# 故障排查

## 无法连接

- 确认 Linux 网关正在运行，且绑定到 loopback 地址。
- 检查 SSH 密钥权限和账户访问权限。
- 检查本地转发端口是否已被占用。

## 心跳一直显示离线

- 确认在心跳轮询之前隧道已连接。
- 确认网关的 `/health` 端点返回 HTTP 200。
- 检查健康状态中的连续失败次数。

## 任务未执行

- 确认 Agent 已绑定到本地节点 ID。
- 确认任务类型为 `system.run`。
- 检查回调结果中的退出码是否为非零值。
